import './Loader.css';

const Loader = () => {
    return (
        <div className="loader">
            <span className="Loader-text">PLEASE WAIT</span>
        </div>
    );
};

export default Loader;